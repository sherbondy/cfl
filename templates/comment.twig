<div class="comment__mod {{comment.comment_type}}" id="comment-{{comment.ID}}">
	<h5 class="comment__meta"><span class="comment__author">{{comment.author.name}}</span> <span class="comment__timestamp">{{comment.date}} at {{comment.time}}</span></h5>
	<div class="comment__content">{{comment.comment_content|wpautop}}</div>
</div>
